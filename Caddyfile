# Replace YOUR_DOMAIN with your actual domain, e.g. finpulse.example.com
# If you don't have a domain yet, use :80 instead (no HTTPS).

{$DOMAIN:localhost} {
    reverse_proxy app:80
    encode gzip
}
